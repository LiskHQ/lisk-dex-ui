import styled from "@emotion/styled";

export const ChartStyle = styled('div')(({ theme }: any) => {
  return {
    width: '100%',
    height: '12.5rem',
    svg: {
      width: '100%',
      height: '12.5rem',
    }
  }
})