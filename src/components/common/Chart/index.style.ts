import styled from '@emotion/styled';

export const ChartStyle = styled('div')(() => {
  return {
    width: '100%',
    height: '12.5rem',
    svg: {
      width: '100%',
      height: '12.5rem',
    }
  };
});